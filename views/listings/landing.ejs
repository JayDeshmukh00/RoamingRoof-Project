<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>ROAMING rOOF</title>
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: #000;
      color: #fff;
      overflow-x: hidden;
    }

    .hero {
      height: 100vh;
      background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)),
        url("https://images.pexels.com/photos/271743/pexels-photo-271743.jpeg") center/cover no-repeat;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      text-align: center;
      animation: fadeIn 2s ease-in;
    }

    .hero h1 {
      font-size: 4.5rem;
      margin: 0;
      text-transform: uppercase;
      letter-spacing: 6px;
      background: linear-gradient(to right, #ffecd2, #fcb69f);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      animation: slideDown 1.2s ease-out;
    }

    .hero p {
      font-size: 1.3rem;
      margin-top: 1rem;
      color: #e0e0e0;
    }

    .description {
      text-align: center;
      padding: 2rem 1rem;
      font-size: 1.1rem;
      color: #ccc;
      max-width: 800px;
      margin: 0 auto;
      line-height: 1.6;
    }

    .music-note {
      text-align: center;
      margin: 3rem 1rem 1rem;
      font-size: 1.2rem;
      font-style: italic;
      color: #ccc;
    }

    .audio-player {
      display: flex;
      justify-content: center;
      margin-bottom: 3rem;
    }

    .audio-player audio {
      width: 300px;
      border-radius: 8px;
    }

    .signup-section {
      text-align: center;
      margin: 4rem 1rem;
    }

    .signup-section h2 {
      font-size: 2rem;
      margin-bottom: 1rem;
      color: #ffecd2;
    }

    .signup-section p {
      color: #bbb;
      margin-bottom: 2rem;
    }

    .signup-section a {
      display: inline-block;
      padding: 1rem 2rem;
      font-size: 1rem;
      text-decoration: none;
      color: #fff;
      background: linear-gradient(to right, #ff7e5f, #feb47b);
      border-radius: 8px;
      box-shadow: 0 6px 16px rgba(0,0,0,0.2);
      transition: transform 0.3s ease;
    }

    .signup-section a:hover {
      transform: scale(1.05);
    }

    .footer {
      text-align: center;
      padding: 2rem 1rem;
      font-size: 0.9rem;
      color: #888;
    }

    @keyframes fadeIn {
      from { opacity: 0 }
      to { opacity: 1 }
    }

    @keyframes slideDown {
      from { transform: translateY(-30px); opacity: 0 }
      to { transform: translateY(0); opacity: 1 }
    }
  </style>
</head>
<body>
  <section class="hero">
    <h1>ROAMING rOOF</h1>
    <p>Developed by Jay Deshmukh</p>
  </section>

  <div class="description">
    Roaming Roof is your gateway to premium property rentals across stunning destinations. Whether you're planning a weekend escape or a long-term stay, we connect you with handpicked homes that blend comfort, style, and unforgettable views. Discover curated listings, immersive visuals, and a seamless booking experience—all in one place.
  </div>

  <div class="music-note">🎵 Groove with our site-anthem</div>
  <div class="audio-player">
    <audio controls autoplay loop>
      <source src="../music/abc.mp3" type="audio/mpeg" />
      Your browser does not support the audio element.
    </audio>
  </div>

  <section class="signup-section">
    <h2>Become a Roaming Roof Insider</h2>
    <p>Get early access to new listings, deals, and exclusive stay experiences.</p>
    <a href="/signup">✨ Sign up — Adventure starts here</a>
  </section>

  <div class="footer">
    &copy; <%= new Date().getFullYear() %> ROAMING rOOF. Curated stays with soul.
  </div>
</body>
</html>