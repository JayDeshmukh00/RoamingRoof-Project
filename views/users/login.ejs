<% layout("/layouts/boilerplate") %>

<style>
  /* --- General Body & Background --- */
  body {
    background-color: #f8f9fa; /* Fallback */
    background-image: linear-gradient(135deg, #ffffff 0%, #f0f2f5 100%);
    font-family: 'Inter', 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif;
  }

  /* --- The Main Authentication Card --- */
  .auth-card {
    background-color: #ffffff;
    border: 1px solid #e9ecef;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.07);
    transition: transform 0.3s ease-in-out;
  }
  .auth-card:hover {
    transform: translateY(-5px);
  }

  /* --- Eye-Catching Gradient Icon (Red like Airbnb) --- */
  .gradient-icon {
    background: linear-gradient(45deg, #FF385C, #E31B23);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-fill-color: transparent;
  }

  /* --- Modern Form Styling (Red like Airbnb) --- */
  .form-control:focus {
    border-color: #FF385C;
    box-shadow: 0 0 0 0.25rem rgba(255, 56, 92, 0.2);
  }

  .form-floating > label {
    color: #6c757d;
  }

  /* --- Button Styling (Red like Airbnb) --- */
  .btn-brand {
    background-color: #FF385C;
    border-color: #FF385C;
    color: #fff;
    padding: 0.75rem 1rem;
    transition: background-color 0.2s ease-in-out, transform 0.2s ease;
  }
  .btn-brand:hover {
    background-color: #E31B23;
    border-color: #C21807;
    color: #fff;
    transform: scale(1.02);
  }
  .link-brand {
      color: #FF385C;
      text-decoration: none;
      font-weight: 600;
  }
  .link-brand:hover {
      color: #E31B23;
  }

</style>

<div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
  <div class="col-lg-5 col-md-8 col-sm-10">

    <div class="card auth-card border-0 rounded-4">
      <div class="card-body p-4 p-md-5">

        <div class="text-center mb-4">
          <i class="fas fa-mountain-sun fa-3x mb-3 gradient-icon"></i>
          <h2 class="fw-bold text-dark">Welcome Back!</h2>
          <p class="text-muted">Log in to continue your adventure.</p>
        </div>

        <form action="/login" method="post" class="needs-validation" novalidate>

          <div class="form-floating mb-3">
            <input name="username" id="username" type="text" class="form-control" placeholder="username" required />
            <label for="username">Username</label>
          </div>

          <div class="form-floating mb-4">
            <input name="password" id="password" type="password" class="form-control" placeholder="password" required />
            <label for="password">Password</label>
          </div>

          <div class="d-grid mt-4">
            <button class="btn btn-brand btn-lg fw-bold" type="submit">Log In</button>
          </div>
        </form>

        <hr class="my-4">

        <div class="text-center">
          <p class="text-muted">
            Don't have an account? <a href="/signup" class="link-brand">Sign Up</a>
          </p>
        </div>

      </div>
    </div>
  </div>
</div>